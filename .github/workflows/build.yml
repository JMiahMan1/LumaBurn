name: LumaBurn Pipeline

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    name: Code Quality (Lint & Format)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
      - run: npm install
      - name: Lint Check
        run: npm run lint
      - name: Format Check
        run: npm run format:check

  test-and-build:
    name: Test & Build Matrix
    needs: quality
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"
      - run: npm install
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium
      - name: Run Unit Tests
        run: npm run test:unit
      - name: Validate udev Rules (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: |
          # Verify rule syntax using udevadm (dry-run)
          udevadm test-builtin uaccess /sys/class/tty/ttyS0 2>&1 || echo "Ignore expected sysfs missing in CI"
          grep "1a86" packaging/linux/99-lumaburn.rules
      - name: Run Functional E2E Tests
        run: npm run test:e2e
      # node-usb rebuilds against Electron and compiles libusb's linux_udev.c (needs libudev.h).
      - name: Install Linux native module build dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libudev-dev pkg-config
      - name: Build Electron App (Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: npx electron-builder --linux
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron App (MacOS)
        if: matrix.os == 'macos-latest'
        run: npx electron-builder --mac
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Electron App (Windows)
        if: matrix.os == 'windows-latest'
        run: npx electron-builder --win
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Linux -- AppImage (x86_64)
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v7
        with:
          path: dist/*x86_64.AppImage
          archive: false

      - name: Upload Linux -- AppImage (arm64)
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v7
        with:
          path: dist/*arm64.AppImage
          archive: false

      - name: Upload Linux -- DEB (amd64)
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v7
        with:
          path: dist/*amd64.deb
          archive: false

      - name: Upload Linux -- DEB (arm64)
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v7
        with:
          path: dist/*arm64.deb
          archive: false

      - name: Upload Linux -- RPM (x86_64)
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v7
        with:
          path: dist/*.x86_64.rpm
          archive: false

      - name: Upload Linux -- RPM (aarch64)
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v7
        with:
          path: dist/*.aarch64.rpm
          archive: false

      - name: Upload MacOS -- DMG
        if: matrix.os == 'macos-latest'
        uses: actions/upload-artifact@v7
        with:
          path: dist/*.dmg
          archive: false

      - name: Upload Windows -- EXE
        if: matrix.os == 'windows-latest'
        uses: actions/upload-artifact@v7
        with:
          path: dist/*.exe
          archive: false
